doctype html
html
	head
		title Longo A MongoDB App
body
	form(method="POST")
		p name
		input(type="text",name="name")
		p job
		input(type="text",name="job")
		p: button Add
hr

if people.length > 0
	ul
		each person in people
			li
				h2= person.name + " - " + person.job
else
	p there are no person